# 为用户安装最新版奶蛙

先阅读 README 的支持范围、安装与升级边界。最新版为 v4.6.1，仅适配 Linux 26.903.61454 的精确构建；macOS 最新问题事件尚未适配，不可套用 Linux 补丁。

以普通用户执行 `python3 install.py check`。验证成功后，确保用户已保存工作并退出 App，再执行 `bash install.sh`。不要自动终止用户正在使用的 App，不要跳过哈希检查，不要覆盖未知旧补丁。旧版安装先使用对应安装器恢复原版。

完成后让用户完全重新打开 App 并选择奶蛙。按 README 的触发规则检查空闲、普通等待、持续思考、悬停、拖动、异步提问及输入大笑。异步提问测试必须保持提问回合运行，等待用户回答再结束。

卸载执行 `bash uninstall.sh`；保留备份、素材来源和许可文件。
